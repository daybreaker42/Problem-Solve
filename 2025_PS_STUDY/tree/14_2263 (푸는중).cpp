#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define SIZE 100000
using namespace std;

// const bool debug = false;
int in_order[SIZE], post_order[SIZE];


int main() {
	fastio;

	// freopen("input.txt", "r", stdin);
    int n;
    cin >> n;
    for (int i = 0; i < n; i++){
        cin >> in_order[i];
    }
    for (int i = 0; i < n; i++){
        cin >> post_order[i];
    }

    // solving

    


	return 0;
}

/* comment 



*/